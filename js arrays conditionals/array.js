var friendsAge = [22, 35, 17, 12, 38];
var picnicFees = [100, 200, 500, 3000];
var vowels = ["a", "e" , "i", "o", "u"];

console.log(vowels);

// Finding the length of an array

console.log(vowels.length);

console.log(friendsAge);

// Output the array elements by the index number
console.log(friendsAge[0]);

// Get elements of an arry by index

var element = friendsAge[2];
console.log(element);

// Set element value by index

friendsAge[1]= 40;
console.log(friendsAge);

// Know the index of an element

var indexOfElement = friendsAge.indexOf(38);
console.log(indexOfElement);
